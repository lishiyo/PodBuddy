<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MyCompatTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/mainBackground</item>
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="actionBarStyle">@style/ActionBar</item>
        <item name="android:homeAsUpIndicator">@drawable/tv_bluegreen</item>
        <item name="android:background">@color/background</item>
        <item name="android:textColor">@color/mainBackground</item>

        <!-- Support library compatibility -->
        <item name="homeAsUpIndicator">@drawable/tv_bluegreen</item>
    </style>

    <style name="ActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/background</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="background">@color/background</item>
    </style>

    <style name="ImageFullWidth">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="CenteredText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/text_title</item>
        <item name="android:layout_margin">@dimen/text_padding</item>
        <item name="android:textColor">@color/genius</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="PodcastTitle">
        <item name="android:textSize">@dimen/text_title</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/mainBackground</item>
    </style>

    <style name="PodcastProducer">
        <item name="android:textSize">@dimen/text_main</item>
        <item name="android:textColor">@color/mainBackground</item>
        <item name="android:clickable">false</item>
    </style>

    <style name="ButtonYellow">
        <item name="android:background">@color/genius</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">false</item>
        <item name="android:textSize">@dimen/text_main</item>
    </style>
</resources>
